const requestOptions = {
  waiting: 'waiting',
  requesting: 'requesting',
  finished: 'finished',
  failed: 'failed',
};

const inputOptions = {
  empty: 'empty',
  valid: 'valid',
  invalid: 'invalid',
  hasUrl: 'hasUrl',
};

export { requestOptions, inputOptions };
